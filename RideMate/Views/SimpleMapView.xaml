<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="RideMate.Views.SimpleMapView">
    
    <!-- Simple Map Display using Grid and Shapes -->
    <Grid x:Name="MapGrid" BackgroundColor="#E8F5E9">
        
        <!-- Map Background -->
        <Image Source="map_background.png" 
               Aspect="AspectFill"
               Opacity="0.3"/>
        
        <!-- Map Content Container -->
        <AbsoluteLayout x:Name="MapContainer">
            <!-- Pins and routes will be added here programmatically -->
        </AbsoluteLayout>
        
        <!-- Center indicator -->
        <Label Text="📍" 
               FontSize="30"
               HorizontalOptions="Center"
               VerticalOptions="Center"
               x:Name="CenterPin"
               IsVisible="False"/>
    </Grid>
</ContentView>
